apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus1.fullname" . }}
  labels:
    {{- include "prometheus1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus1.selectorLabels" . | nindent 4 }}
